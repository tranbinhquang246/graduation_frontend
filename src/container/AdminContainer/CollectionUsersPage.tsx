import React from "react";
import { connect } from "react-redux";

export const CollectionUsersPage = () => {
  return <div>CollectionUsersPage</div>;
};

const mapStateToProps = () => ({});

const mapDispatchToProps = {};

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(CollectionUsersPage);
